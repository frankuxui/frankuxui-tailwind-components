---
title: Pepe
group: components
description: Muestra un diálogo de alerta con un mensaje opcional, y aguardará hasta que el usuario cierre la ventana de diálogo.
---

### My first post


```jsx live=true
() => {
  const [likes, increaseLikes] = React.useState(0);

  return (
    <div>
      <h1>
        {`${likes} likes`}
      </h1>
      <button className='py-2 px-4 bg-red-400' onClick={() => increaseLikes(likes + 1)}>Like</button>
    </div>
  );
}
```

```jsx live
() => {
  return (
    <div>
      <h1>Hello World Example</h1>
    </div>
  );
};
```

